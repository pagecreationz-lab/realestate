SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SECRET_KEY=sb_secret_replace-me
JWT_SECRET=replace-with-a-long-random-secret
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# 64 hexadecimal characters (32 random bytes). Keep stable to decrypt existing withdrawal details.
BANK_DETAILS_KEY=replace-with-64-hex-characters
# SMTP is configured in Super Admin Settings. Keep JWT_SECRET stable to decrypt saved SMTP passwords.

# Shared Google Drive account (server-only OAuth credentials).
GOOGLE_DRIVE_CLIENT_ID=
GOOGLE_DRIVE_CLIENT_SECRET=
GOOGLE_DRIVE_REDIRECT_URI=http://localhost:3000/api/drive-callback
